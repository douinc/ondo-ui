---
title: 설치
description: 새 프로젝트 또는 기존 프로젝트에 Ondo UI를 설치합니다.
---

import {
  IconBinaryTree,
  IconBraces,
  IconBrandAstro,
  IconBrandLaravel,
  IconBrandNextjs,
  IconBrandReact,
  IconBrandVite,
} from "@tabler/icons-react"

<Callout className="mb-6 border-emerald-600 bg-emerald-100 dark:border-emerald-400 dark:bg-emerald-900">

**새 프로젝트에 권장:** framework를 shadcn CLI로 초기화한 다음 `@ondo-ui` 레지스트리를 등록하고 테마를 설치하세요.

</Callout>

## CLI 사용

CLI로 지원되는 프로젝트를 바로 만들 수 있습니다:

```bash
bunx shadcn@latest init -t [framework]
```

지원 템플릿은 `next`, `vite`, `start`, `react-router`, `astro`입니다. Laravel은 먼저 `laravel new`로 앱을 만든 뒤 `bunx shadcn@latest init`을 실행하세요.

## 기존 프로젝트

이미 프로젝트가 있다면 아래에서 framework를 선택하고 해당 페이지의 레지스트리 설정을 따라 하세요.

## Framework 선택

<div className="mt-8 grid gap-4 sm:grid-cols-2 sm:gap-6">
  <LinkedCard href="/ko/docs/installation/next">
    <IconBrandNextjs className="h-10 w-10" aria-hidden="true" />
    <p className="mt-2 font-medium">Next.js</p>
  </LinkedCard>
  <LinkedCard href="/ko/docs/installation/vite">
    <IconBrandVite className="h-10 w-10" aria-hidden="true" />
    <p className="mt-2 font-medium">Vite</p>
  </LinkedCard>
  <LinkedCard href="/ko/docs/installation/tanstack">
    <IconBinaryTree className="h-10 w-10" aria-hidden="true" />
    <p className="mt-2 font-medium">TanStack Start</p>
  </LinkedCard>
  <LinkedCard href="/ko/docs/installation/laravel">
    <IconBrandLaravel className="h-10 w-10" aria-hidden="true" />
    <p className="mt-2 font-medium">Laravel</p>
  </LinkedCard>
  <LinkedCard href="/ko/docs/installation/react-router">
    <IconBrandReact className="h-10 w-10" aria-hidden="true" />
    <p className="mt-2 font-medium">React Router</p>
  </LinkedCard>
  <LinkedCard href="/ko/docs/installation/astro">
    <IconBrandAstro className="h-10 w-10" aria-hidden="true" />
    <p className="mt-2 font-medium">Astro</p>
  </LinkedCard>
  <LinkedCard href="/ko/docs/installation/manual">
    <IconBraces className="h-10 w-10" aria-hidden="true" />
    <p className="mt-2 font-medium">수동 설치</p>
  </LinkedCard>
</div>

## 사전 준비

ondo-ui 컴포넌트는 [shadcn CLI](https://ui.shadcn.com/docs/installation)로 설정된 프로젝트를 전제로 합니다 — Tailwind CSS v4, `components.json`, `cn()` 유틸리티. 새 프로젝트라면 위에서 framework를 선택해 먼저 프로젝트를 초기화하세요.

## 레지스트리 등록

`components.json`의 `registries` 필드에 `@ondo-ui` 네임스페이스를 추가합니다:

```json title="components.json"
{
  "registries": {
    "@ondo-ui": "https://ui.ondo.dou.so/r/{name}.json"
  }
}
```

## 테마 추가

먼저 ondo 테마를 설치하세요 — Pretendard·Monaspace Neon 폰트와 기본 CSS 변수가 함께 들어옵니다:

```bash
bunx shadcn@latest add @ondo-ui/theme @ondo-ui/theme-provider
```

## 컴포넌트 추가

이름으로 원하는 컴포넌트를 추가합니다. `utils` 같은 의존성은 자동으로 해결됩니다:

```bash
bunx shadcn@latest add @ondo-ui/button
```

그다음 프로젝트에서 import 하세요:

```tsx
import { Button } from "@/components/ui/button"

export default function Example() {
  return <Button>클릭</Button>
}
```
