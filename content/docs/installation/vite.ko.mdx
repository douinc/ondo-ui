---
title: Vite
description: Vite에 Ondo UI를 설치하고 설정합니다.
---

시작점에 맞는 설정 방법을 선택하세요.

<div className="mt-6 grid gap-4 sm:grid-cols-2 sm:gap-6">
  <LinkedCard href="#scaffold-with-cli" className="items-start gap-1 p-6 text-sm md:p-6"><div className="font-medium">CLI 사용</div><div className="leading-relaxed text-muted-foreground">터미널에서 새 Vite 프로젝트를 생성합니다.</div></LinkedCard>
  <LinkedCard href="#existing-vite-project" className="items-start gap-1 p-6 text-sm md:p-6"><div className="font-medium">기존 프로젝트</div><div className="leading-relaxed text-muted-foreground">기존 Vite 프로젝트에 Ondo UI를 직접 설정합니다.</div></LinkedCard>
</div>

<div id="scaffold-with-cli" className="scroll-mt-24" />
## CLI 사용

<Steps>

### 프로젝트 만들기

```bash
npx @dou.so/ondo-ui@latest init -t vite
```

명령이 Ondo 테마와 `@ondo-ui` 레지스트리를 자동으로 설정합니다. 추가 컴포넌트는 다음처럼 설치합니다:

```bash
npx shadcn@latest add @ondo-ui/button
```

</Steps>

<div id="existing-vite-project" className="scroll-mt-24" />
## 기존 프로젝트

<Steps>

### 프로젝트 만들기

```bash
npm create vite@latest my-app -- --template react-ts
cd my-app
npm install
```

### Tailwind CSS 설정

```bash
npm install tailwindcss @tailwindcss/vite
```

`vite.config.ts`에 플러그인을 추가하고 `src/index.css`에 Tailwind를 import합니다:

```ts title="vite.config.ts"
import tailwindcss from "@tailwindcss/vite"

export default { plugins: [tailwindcss()] }
```

```css title="src/index.css"
@import "tailwindcss";
```

### Import Alias 설정

```json title="tsconfig.json"
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  }
}
```

### CLI 실행 및 registry 등록

```bash
npx shadcn@latest init
```

```json title="components.json"
{
  "registries": {
    "@ondo-ui": "https://ui.ondo.dou.so/r/{name}.json"
  }
}
```

### 테마와 컴포넌트 추가

```bash
npx shadcn@latest add @ondo-ui/theme @ondo-ui/theme-provider
npx shadcn@latest add @ondo-ui/button
```

[테마 가이드](/docs/theming)에 따라 `ThemeProvider`를 설정할 수 있습니다.

### 컴포넌트 사용

```tsx
import { Button } from "@/components/ui/button"

export default function Example() {
  return <Button>클릭</Button>
}
```

</Steps>
